spring:
  application:
    name: PRODUCT-SERVICE
  cloud:
    config:
#      fail-fast: true
#      retry:
#        max-attempts: 20
#        max-interval: 15000
#        initial-interval: 10000
      discovery:
        service-id: CONFIG-SERVER
        enabled: true